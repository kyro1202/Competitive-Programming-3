#include <bits/stdc++.h>
using namespace std;

int main()
{
    string a,b,ans;
    while(true)
    {
        cin >> a;
        if(!a.length())
            break;
        cin >> b;
        set<string> per;
        ans = "";
        
    }
}