#include <bits/stdc++.h>
using namespace std;
int stores,roads,operas;
bool avail[51][13];
vector<vector<pair<int,double> > > adj;
int main()
{
    ios::sync_with_stdio(false);
    int test; cin >> test;
    while(test--)
    {
        cin >> stores >> roads;
        adj.resize(stores+1,vector<pair<int,double> >());
        
    }
}